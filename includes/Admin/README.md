# Admin

Admin classes should own WordPress admin pages, screens and UI-specific controllers.

The current admin UI is still handled by the legacy Plugin class. New 1.3 work should move here gradually.
